# Speckit: Intent-Engine "Guardian" Integration Protocol

**版本**: 2.0
**目标**: 解决 AI Agent 在长期、跨会话任务中对 Intent-Engine 的"意图遗忘"和"状态分裂"问题。
**核心哲学**: 通过渐进式的上下文强化机制，确保 Intent-Engine 始终是人机协作的"唯一事实来源"。
**方法论**: 分阶段验证，从轻量级方案开始，逐步演进到更复杂的架构。

---

## 1. 问题分析

### 1.1 核心问题：长上下文记忆衰减

**观察数据**: 在50轮以上的长对话中，AI Agent 有约 **5% 的概率遗忘会话初期的信息**。

**根本原因**: AI 的注意力机制天然地对**最近的 prompt 赋予更高权重**，而对会话开始时的系统提示权重逐渐衰减。

```
会话开始                工作进行中...                         会话末尾
    ↓                      ↓                                    ↓
[系统提示]  ────→  [50轮对话后] ────→ [注意力集中在最近内容]
  Intent-Engine           ↓                         ↓
  很重要！            权重降低...              几乎被遗忘了
```

### 1.2 具体表现

1. **跨会话遗忘**: 新会话开始时，AI 不知道上次的工作状态
2. **会话内遗忘**: 在同一会话的后期，AI 倾向于使用即时工具(TodoWrite)而非战略工具(Intent-Engine)
3. **状态分裂**: 战术进展(TodoWrite完成的任务)未同步到战略记录(Intent-Engine)中

### 1.3 解决思路

**核心策略**: 持续强化 Intent-Engine 在 AI 工作记忆中的存在感，让它始终出现在"最近的上下文"中。

**实现路径**: 分阶段验证，避免过度设计。

---

## 2. 分阶段实施方案

### Phase 1: 焦点恢复 (会话开始时重建上下文) ✅

**可行性**: 已验证 - Claude Code 支持 SessionStart Hook
**侵入性**: 无侵入 - 纯外部实现
**实施优先级**: 立即实施

#### 2.1 实现方式

利用 `.claude/hooks/session-start.sh` 或类似机制：

```bash
#!/bin/bash
# .claude/hooks/session-start.sh

# 检查是否存在当前焦点
CURRENT=$(ie current --json 2>/dev/null)

if [ $? -eq 0 ] && [ "$(echo $CURRENT | jq -r '.current_task_id')" != "null" ]; then
    TASK_ID=$(echo $CURRENT | jq -r '.current_task_id')
    CONTEXT=$(ie task context --json)

    # 构建焦点恢复提示
    TASK_NAME=$(echo $CONTEXT | jq -r '.task.name')
    PARENT_NAME=$(echo $CONTEXT | jq -r '.parent.name // "None"')
    TODO_CHILDREN=$(echo $CONTEXT | jq -r '.children | map(select(.status == "todo")) | length')

    echo "<system-reminder priority=\"high\">"
    echo "## Intent-Engine: Focus Restoration ##"
    echo ""
    echo "Welcome back. Your strategic focus has been restored."
    echo ""
    echo "**CURRENT FOCUS**: #${TASK_ID} '${TASK_NAME}'"
    echo "**MISSION**: ${PARENT_NAME}"
    echo "**REMAINING SUBTASKS**: ${TODO_CHILDREN}"
    echo ""
    echo "Your primary objective is to continue making progress on the **CURRENT FOCUS**."
    echo "</system-reminder>"
else
    echo "<system-reminder>"
    echo "## Intent-Engine: Welcome ##"
    echo ""
    echo "No active focus. Use \`ie pick-next\` to find your next task."
    echo "</system-reminder>"
fi
```

#### 2.2 预期效果

- ✅ 解决跨会话遗忘问题
- ✅ 每次会话开始时，AI 立即知道当前的战略状态
- ⚠️ 无法解决会话内的长上下文记忆衰减

---

### Phase 2: 结构化输出 + 轻量级提醒 (持续强化) 🔄

**可行性**: 待验证 - 需要少量修改 Intent-Engine
**侵入性**: 低侵入 - 仅增强输出，不改变核心逻辑
**实施优先级**: Phase 1 验证后，如效果不足则实施

#### 2.3 核心设计原则

**原则 1: 数据与提示分离**
- Intent-Engine 输出结构化的"下一步建议"数据
- 上下文提醒可选地附加在输出中
- 不硬编码 prompt 文本，使用模板化方式

**原则 2: 分级提醒策略**
- **Silent**: 查询类命令(list, search) - 不提醒
- **Gentle**: 轻量操作(event add) - 轻提醒
- **Standard**: 状态变更(start, switch) - 标准提醒
- **Strong**: 关键节点(done, spawn) - 强提醒

**原则 3: 自然可见**
- 即使宿主环境不支持系统提示注入
- 提醒内容也应在标准输出中对 AI 可见
- 作为 JSON 输出的一部分或后续文本

#### 2.4 JSON 输出结构设计

```json
{
  "completed_task": { "id": 43, "status": "done" },
  "workspace_status": { "current_task_id": null },

  // 结构化的下一步建议 (给AI理解的数据)
  "next_step": {
    "action": "start_parent",
    "task_id": 42,
    "task_name": "Implement authentication",
    "reason": "all_children_complete",
    "priority": "high",
    "context": {
      "parent_subtasks_remaining": 0,
      "all_siblings_done": true
    }
  },

  // 可选的上下文提醒 (用于强化记忆)
  "context_reminder": {
    "level": "strong",
    "tool": "Intent-Engine",
    "message": "Task #43 complete. Focus cleared. Parent #42 is ready.",
    "suggestion": "Consider: ie task start 42"
  }
}
```

#### 2.5 实现方式

**方式A: 标准输出后附加**
```rust
// 在命令执行完成后
println!("{}", json_output);

if let Some(reminder) = context_reminder {
    eprintln!(); // 空行分隔
    eprintln!("📍 Intent-Engine: {}", reminder.message);
    if let Some(suggestion) = reminder.suggestion {
        eprintln!("   Suggestion: {}", suggestion);
    }
}
```

**方式B: JSON 中嵌入**（如果宿主环境支持解析）
- 宿主环境可以提取 `context_reminder` 字段
- 将其渲染为 `<system-reminder>` 注入到下次对话
- 从返回给 AI 的 JSON 中移除该字段

#### 2.6 提醒内容模板

```rust
enum ReminderTemplate {
    TaskComplete {
        task_id: u32,
        next_action: NextAction,
    },
    FocusChanged {
        from_id: u32,
        to_id: u32,
        reason: String,
    },
    SubtaskSpawned {
        parent_id: u32,
        child_id: u32,
    },
}

impl ReminderTemplate {
    fn render(&self, level: ReminderLevel) -> String {
        match (self, level) {
            (TaskComplete { next_action: StartParent(id), .. }, Strong) =>
                format!("Task complete. Parent #{} is ready. All prerequisites done.", id),
            (TaskComplete { next_action: PickNext, .. }, Standard) =>
                format!("Task complete. Focus cleared. Use 'ie pick-next' to continue."),
            // ...
        }
    }
}
```

#### 2.7 预期效果

- ✅ 在每次关键操作后，Intent-Engine 重新出现在"最近的上下文"
- ✅ AI 通过持续的"存在感"保持对 Intent-Engine 的记忆
- ✅ 提醒内容简洁、有价值，不会造成 prompt 疲劳
- ⚠️ 仍然依赖单个 Agent 的长上下文能力

---

### Phase 3: Sub-Agent 架构 (任务级上下文隔离) 🚀

**可行性**: 待探索 - 取决于宿主环境的 Agent 管理能力
**侵入性**: 架构级变化 - 但对 Intent-Engine 本身无侵入
**实施优先级**: Phase 2 仍不足时，作为终极方案

#### 2.8 核心思想

**彻底解决长上下文问题的方法：不要有长上下文。**

每个 Intent-Engine 任务启动一个专属的 Sub-Agent，每个 Agent 只工作 10-30 轮，从根本上避免记忆衰减问题。

**详细设计**: 参见 [`sub-agent-architecture.md`](./sub-agent-architecture.md)

**核心特性**:
- ✅ Main Agent 采用**混合模式**：简单任务自己处理，复杂任务分派 Sub-Agent
- ✅ 每个 Sub-Agent 专注单一任务，生命周期 10-30 轮
- ✅ 状态持久化在 Intent-Engine 中，支持跨会话
- ✅ 对 Intent-Engine 本身零侵入


---

## 3. 守护组件：智能审计 (可选增强)

### 3.1 目标

检测 AI 在使用其他工具时的战术进展，提醒其同步到 Intent-Engine。

### 3.2 触发机制

**前置条件**: 宿主环境提供 `PostToolUse` Hook

**审计规则**:

| 检测到的行为 | 触发条件 | 提醒内容 |
|------------|---------|---------|
| 战术进展 | TodoWrite 标记任务为 completed | "Consider: `ie event add --type milestone`" |
| 代码变更 | writeFile/patchFile | "If this is a key decision: `ie event add --type decision`" |
| 持续调研 | 连续3+次 readFile | "Capture findings: `ie event add --type note`" |

### 3.3 实施优先级

- 如果 Phase 1 + Phase 2 已足够，可以不实施
- 这是一个"锦上添花"的功能，不是必需的

---

## 4. 实施路线图

### 第一阶段：验证基础假设 (本周)

- [ ] 实现 Phase 1 (焦点恢复)
- [ ] 在真实工作场景中测试
- [ ] 观察: AI 是否在新会话中正确使用 Intent-Engine
- [ ] 观察: 长会话后期(50轮+)是否仍记得 Intent-Engine

### 第二阶段：持续强化 (如第一阶段不足)

- [ ] 实现 Phase 2 (结构化输出 + 提醒)
- [ ] 选择实现方式(标准输出 or JSON 嵌入)
- [ ] 测试提醒频率是否合适
- [ ] 测量: Intent-Engine 使用率是否提升

### 第三阶段：架构探索 (如第二阶段仍不足)

- [ ] 调研宿主环境的 Sub-Agent 支持能力
- [ ] 设计 Main Agent ↔ Sub-Agent 交互协议
- [ ] 原型验证
- [ ] 如可行，全面实施

---

## 5. 成功指标

### 定量指标

- **Intent-Engine 使用率**: 在50轮+的长会话中，AI 主动使用 Intent-Engine 的频率
- **状态同步率**: TodoWrite 中完成的任务，有多少被同步到 Intent-Engine (via event add)
- **跨会话连续性**: 新会话开始时，AI 正确引用上次任务的比例

### 定性指标

- AI 是否展现出"战略思维"(优先使用 Intent-Engine)
- AI 是否能在长会话后期仍记得 Intent-Engine 的存在
- 用户是否感觉 AI 的工作更加"有条理"和"有记忆"

---

## 6. 技术依赖清单

| 组件 | 依赖能力 | Claude Code 支持? | 备选方案 |
|-----|---------|------------------|---------|
| Phase 1: 焦点恢复 | SessionStart Hook | ✅ 确认支持 | N/A (已可用) |
| Phase 2: 结构化输出 | 标准输出控制 | ✅ 无需特殊支持 | N/A |
| Phase 2: JSON 注入 | 工具输出后处理 | ❓ 待确认 | 降级到标准输出 |
| Phase 3: Sub-Agent | Agent 生命周期管理 | ❓ 待调研 | 考虑自建 `ie agent` 命令 |
| 智能审计 | PostToolUse Hook | ❓ 待确认 | 不实施 |

---

## 7. 设计哲学总结

### 核心原则

1. **渐进式验证**: 从简单方案开始，逐步增加复杂度
2. **最小侵入**: 优先使用外部机制，避免修改核心
3. **数据驱动**: 通过实际效果决定是否进入下一阶段
4. **优雅降级**: 即使宿主环境不支持高级特性，基础功能仍可用

### 关键洞察

**问题不在于 Intent-Engine 本身，而在于 AI 的工作记忆特性。**

我们不是要"修复" Intent-Engine，而是要适配 AI 的认知特点：
- 注意力集中在最近的上下文
- 长期记忆需要持续强化
- 专注的短上下文优于分散的长上下文

### Sub-Agent 的哲学意义

**最好的记忆强化，就是不需要记忆。**

与其让一个 Agent 在 100 轮对话中努力记住最初的目标，不如让每个任务都有一个专注的、短生命周期的 Agent。这是一种**"上下文重置"**策略，用架构设计解决认知限制。

---

**文档版本**: 2.0
**最后更新**: 2025-11-13
**状态**: 分阶段实施方案 - Phase 1 就绪
